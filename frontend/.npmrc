# The committed pnpm-lock.yaml is the source of truth and is reviewed on update,
# so disable pnpm's minimum-release-age supply-chain gate: it adds non-deterministic
# build failures when the lockfile pins a transitive dep (e.g. electron-to-chromium)
# published within the default age window, even on a --frozen-lockfile install.
minimum-release-age=0
